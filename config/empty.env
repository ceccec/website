# Intentionally empty. Passed to `wrangler types --env-file` so the generated
# cloudflare-env.d.ts is deterministic (bindings-only) and never leaks variable
# names from a developer's local .env / .dev.vars (both gitignored, absent in CI).
