/**
 * Global document slugs — must match `GlobalConfig.slug` and `Config['globals']` keys in generated types.
 * @see https://payloadcms.com/docs/configuration/globals
 */
export const PUBLIC_SITE_SETTINGS_SLUG = 'public-site-settings'
export const INTEGRATION_SECRETS_SLUG = 'integration-secrets'
